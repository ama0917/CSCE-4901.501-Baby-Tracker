# Web framework
Flask>=2.2,<3.0

# Firebase Admin SDK (includes Firestore support)
firebase-admin>=6.2.0,<7.0

# Google Cloud dependencies (used internally by firebase-admin)
google-cloud-firestore>=2.15.0,<3.0
google-api-core>=2.11.0

# Database (SQLite is built into Python, no package needed)

# Utilities
requests>=2.28.0
python-dotenv>=0.21.0

# Testing
pytest>=7.0
